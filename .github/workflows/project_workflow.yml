# GitHub Actions training


name: Project-workflow
  
  
on:
  push:
    branches:
      - master
      
      
jobs:
  deploy:
    runs-on: ubuntu-latest
    container: node:19
    steps:
      -  uses: actions/checkout@v1
      -  run: npm install
      -  run: npm start
         
